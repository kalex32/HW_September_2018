package DZ2_Collections;

class WebServer {
    private boolean webServer;

    WebServer(boolean webServer) {
        this.webServer = webServer;
    }

    boolean isWebServer() {
        return webServer;
    }
}
